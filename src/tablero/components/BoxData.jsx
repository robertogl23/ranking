import Range from './Range'

const BoxData = ({score}) => {
    return (
        <div style={{width:'100%'}}>                   
            <Range  score={score}/>
        </div>
    )
}

export default BoxData
